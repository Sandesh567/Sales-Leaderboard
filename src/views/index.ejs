<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="refresh" content="30">
    <title>Sales Leaderboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background-color: #f4f7f6; font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; }
        .container { margin-top: 50px; max-width: 900px; }
        .leaderboard-header { background: #2c3e50; color: white; padding: 20px; border-radius: 10px 10px 0 0; }
        .table-container { background: white; padding: 20px; border-radius: 0 0 10px 10px; box-shadow: 0 4px 15px rgba(0,0,0,0.1); }
        .rank-1 { background-color: #fff9c4 !important; font-weight: bold; border-left: 5px solid #ffd700; }
        .rank-2 { background-color: #f5f5f5 !important; }
        .medal { font-size: 1.2rem; margin-right: 5px; }
    </style>
</head>
<body>
   <div class="container">
    <div class="leaderboard-header text-center">
        <h1>🏆 Sales Leaderboard</h1>
        <p class="mb-0">Real-time performance rankings</p>
    </div>
    
    <div class="table-container">
        <table class="table table-hover align-middle">
            <thead class="table-light">
                <tr>
                    <th scope="col">Rank</th>
                    <th scope="col">Agent Name</th>
                    <th scope="col" class="text-end">Total Sales</th>
                    <th scope="col" class="text-center">Deals</th>
                </tr>
            </thead>
            <tbody>
                <% if (agents.length === 0) { %>
                    <tr>
                        <td colspan="4" class="text-center py-4">No sales data recorded yet.</td>
                    </tr>
                <% } %>

                <% agents.forEach(function(agent) { %>
                    <tr class="<%= agent.rank === 1 ? 'rank-1' : '' %>">
                        <td>
                            <% if (agent.rank === 1) { %>
                                <span class="medal">🥇</span> 1st
                            <% } else if (agent.rank === 2) { %>
                                <span class="medal">🥈</span> 2nd
                            <% } else if (agent.rank === 3) { %>
                                <span class="medal">🥉</span> 3rd
                            <% } else { %>
                                <%= agent.rank %>th
                            <% } %>
                        </td>
                        <td><%= agent.name %></td>
                        <td class="text-end">Rs <%= agent.totalSales.toLocaleString() %></td>
                        <td class="text-center"><%= agent.totalDeals %></td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    </div>
    
</div>

</body>
</html>
</body>
</html>